byte main() {
   return recursion(1);
};

byte recursion(byte param) {
   when (param >= 10) then {
      print param;
      return param;
   } else {
      print param;
      return recursion(param+1);
   };
};
